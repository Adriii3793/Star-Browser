// Preset avatars selectable during setup.
//
// Every entry here is preloaded by the loading screen before onboarding starts,
// so the profile step never shows a flash of missing image. Add a file to
// static/avatars/ and a matching line here — this list is the single source of
// truth for both the preload and the picker.
export const PRESET_AVATARS: string[] = ['/avatars/default.svg'];
